# Non-secret production defaults shipped with every rolling deployment.
# Server-specific credentials remain in /root/nubase/nubase.env and override these values.
SPRING_PROFILES_ACTIVE="prod"
NUBASE_AI_GATEWAY_IMAGE_DEFAULT_MODEL="gpt-image-2"
